let nota = 5.2 
let situacao 
/*
if(nota >= 6){
    situacao = 'Aprovado'
} 
else {
    situacao = 'Reprovado'
}

console.log(situacao)*/

situacao = (nota >= 6 ? 'Aprovado' : 'Reprovado') //vendo forma de fazer a condição mais simples
console.log(situacao)

let use ='admin'
let userType

userType = user=='admin' || user ==='root' ? 'Superuser' : 'Ordinary user'

console.log(userType)